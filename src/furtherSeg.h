#ifndef FURTHERSEG_H
#define FURTHERSEG_H

// Determine if needs further steps of segmentation
void furtherSeg( int *ptr_hgg, const int &len,
                 const int *ptr_seg, const double &prop );

#endif