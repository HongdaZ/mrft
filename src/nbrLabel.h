#ifndef NBRLABEL_H
#define NBRLABEL_H

#include <vector>

using std::vector;

vector<int> nbrLabel( const int curr_idx, 
                 const int *ptr_seg, const int *ptr_nidx );
                 
#endif
